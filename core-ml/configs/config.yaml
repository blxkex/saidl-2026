model:
  context_len: 1024
  embed_dim: 128
  n_heads: 4
  n_layers: 4
  dropout: 0.1

training:
  lr: 3e-4
  batch_size: 4
  epochs: 50
  device: "cuda"

data:
  dataset_path: "wikitext"
  dataset_name: "wikitext-2-raw-v1"